// When changing this, make sure to modify the Makefile accordingly
rootProject.name = "pumpkin-plugin-template-kt"

pluginManagement {
    resolutionStrategy {
        repositories {
            gradlePluginPortal()
        }
    }
}
